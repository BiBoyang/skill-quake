你在协助一位用户完成 Agent Skill 评测任务。严格按以下背景与请求行事，不要自行扩展范围。

背景：
- 用户安装了一个名为 skill-upper 的 Agent Skill，目录是 __SKILL_PATH__（入口为其中的 SKILL.md）。该目录对你只读，禁止修改其中任何文件。
- skill-up CLI 已安装在 PATH 上（可用 command -v skill-up 验证；若未安装，先按官方 install.sh 安装或从源码 go build）。
- 用户的 Skill 已复制到 __WORKDIR__/target/（含 SKILL.md，可直接读写）。

用户的原话请求：
「这是我的 skill。帮我用 skill-up 的流程给它设计一套评测（evals），并用 skill-up validate 校验配置。机器上没有 Agent Engine 凭据，所以不要真正运行评测（不要执行 skill-up run），到 validate 通过为止。最后给我一个评测方案总结。」

要求：
- 第一步先读 __SKILL_PATH__/SKILL.md，之后严格遵循它的流程与指示。
- 所有写入只允许发生在 __WORKDIR__/ 之内。
- 完成后返回完整报告：实际执行的步骤、生成/修改的文件清单（附关键内容摘要）、validate 的命令与输出、对 target skill 的评测方案总结。过程中若观察到任何异常或不一致，如实写入报告，不要隐瞒。
